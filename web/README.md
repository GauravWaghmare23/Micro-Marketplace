# Web - Micro Marketplace

## Setup

1. Install dependencies

```bash
cd web
npm install
```

2. Run dev server

```bash
npm run dev
```

Environment:
- `VITE_API_URL` - backend API base, default `http://localhost:4000`

Screens:
- Home (product list)
- Product detail
- Login / Register

